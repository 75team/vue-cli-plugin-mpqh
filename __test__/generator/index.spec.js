function sum (a, b) {
  return a + b
}

test('add 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3)
})

// const a = require('../../generator/index')
it('generator: resolveFile', () => {
  // resolveFile('../../generator/template')
})
